name = "thatplace-vision"
main = "worker.js"
compatibility_date = "2024-11-01"
workers_dev = true

# GEMINI_KEY is set as a SECRET (not here): `wrangler secret put GEMINI_KEY`
# ALLOWED_ORIGIN is optional; set it to your hosted app origin to limit abuse:
# [vars]
# ALLOWED_ORIGIN = "https://your-app.pages.dev"
